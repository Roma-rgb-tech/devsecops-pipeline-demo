receivers:
  otlp:
    protocols:
      grpc:
  filelog:
    include:
      - /var/log/otel/collector.log
    file_format:
      type: json

exporters:
  otlp:
    endpoint: "tempo:4317"
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]